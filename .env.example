################################################################################
# WORLD FOOD RECIPES - ENVIRONMENT CONFIGURATION
# Final Production Configuration for Cloudflare Pages
# December 12, 2025
################################################################################
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local for local development
# 2. For Cloudflare Pages production deployment:
#    - Go to: Pages > worldfoodrecipes.sbs > Settings > Environment Variables
#    - Add each variable below (REQUIRED ONLY section - 11 variables)
#    - Cloudflare automatically handles NODE_ENV (don't set manually)
#
# SECURITY:
# - NEVER commit .env.local to Git
# - NEVER expose GITHUB_TOKEN or FIREBASE_PRIVATE_KEY
# - Rotate tokens every 90 days
# - Keep Firebase service account keys in secure vault
#
################################################################################

####################
# REQUIRED (11 Variables) - MUST SET
####################

# Admin Dashboard Password
# Used by: Authentication system (/admin/login)
# Min: 8 characters, use strong password
ADMIN_PASSWORD=your-strong-admin-password

# GitHub Repository Configuration
GITHUB_OWNER=erolledph
GITHUB_REPO=nextjs-standard-pwa-main
GITHUB_TOKEN=ghp_your_token_here


# Firebase Admin SDK (Server-side secrets - NEVER expose to client)
FIREBASE_PROJECT_ID=chef-ai-nunoy
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYourKeyHere\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@chef-ai-nunoy.iam.gserviceaccount.com

# Gemini AI API (Server-side only)
GEMINI_API_KEY=your_gemini_api_key_here

# Public Site Configuration
NEXT_PUBLIC_SITE_URL=https://worldfoodrecipes.sbs

# Firebase Client Configuration (PUBLIC - Client-safe)
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyB2grFk9IQj7rP-gMrYVNuLRHpdQiEoVAo
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=chef-ai-nunoy.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=chef-ai-nunoy
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=chef-ai-nunoy.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=132815866728
NEXT_PUBLIC_FIREBASE_APP_ID=1:132815866728:web:27a585e91ce140062724dd
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-J44SP6VF1N

####################
# CLOUDFLARE PAGES DEPLOYMENT
####################

# Step 1: Ensure .env.local is in .gitignore (never commit secrets)
# Step 2: Go to Cloudflare Pages > worldfoodrecipes.sbs > Settings
# Step 3: Environment Variables > Add all 11 REQUIRED variables above
# Step 4: Set environment to "Production"
# Step 5: Redeploy - Cloudflare automatically uses new variables

# ✅ Cloudflare automatically handles:
# - NODE_ENV (set to 'production' automatically)
# - HTTPS/SSL (automatically managed)
# - Caching headers (configured in next.config.mjs)

# DO NOT set these manually:
# - NODE_ENV (Cloudflare handles this)
# - NEXT_PUBLIC_AI_CHEF_ENABLED (not used)
# - LOG_LEVEL (optional, defaults to 'info')

####################
# VALIDATION CHECKLIST
####################

# Before deploying, verify:
# ✓ pnpm tsc --noEmit (TypeScript check)
# ✓ pnpm build (Production build)
# ✓ ADMIN_PASSWORD: 8+ characters, secure
# ✓ GITHUB_TOKEN: Valid, unexpired, correct scopes
# ✓ FIREBASE_PRIVATE_KEY: Valid format with newlines preserved
# ✓ NEXT_PUBLIC_SITE_URL: https://worldfoodrecipes.sbs
# ✓ All 11 variables set in Cloudflare

# Tips:
# - Test locally: pnpm dev (uses .env.local)
# - Build for production: pnpm build
# - Check for errors: Look for "Environment variable X is not set"
# - Rotate GITHUB_TOKEN every 90 days
# - Keep Firebase keys in secure secret manager
